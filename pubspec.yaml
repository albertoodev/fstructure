name: fstructure
description: A CLI tool to generate Flutter project structure (clean architecture) with state management options 
repository: https://github.com/albertoodev/fstructure
version: 1.0.0

environment:
  sdk: ^3.10.4

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  args: ^2.7.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  fs : bin/fs.dart
